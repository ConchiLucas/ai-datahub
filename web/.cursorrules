# AI Frontend Coding Rules & Skills

## 1. 基础说明
此文件旨在为 AI 代码助手提供针对 `ai-note/web`（前端）的工作规范与默认阅读上下文。

## 2. 核心架构与技术栈
- **语言**：TypeScript / TSX
- **框架**：React / Vite
- **UI & 样式**：[请填写你的主要样式方案，例如 TailwindCSS, css-modules, Ant Design 等]
- **状态管理**：[请填写你的状态机，例如 Zustand, Redux, Context API 等]

## 3. 专属规范 (Norms)
- **代码分割**：[填写抽取组件和 Custom Hook 的准则]
- **视觉体验**：所有的界面设计需要追求极佳体验（包含微动画、深色模式兼容等）。
- **API 请求**：[填写对 api 层的封装规范，比如 axios 拦截器的错误抛出方式]

## 4. 特殊技能与避坑 (Skills & Gotchas)
- [填空] 例如：“所有的请求 URL 都需要前置加上 /api/v1”
- [填空] 例如：“遇到 `useEffect` 时必须严格检查依赖数组，避免死循环”

## 5. AI 自测与执行策略 (Execution & Testing Skills)
> **非常重要：前端服务自测规范**
- **默认端口**：前端默认运行在 `5173` 端口。当我被要求使用浏览器验证时，默认针对 `http://localhost:5173` 测试。
- **【当有新增依赖需要安装时】**：
  1. 必须先停掉正在占用 `5173` 端口的进程。
  2. 执行命令安装你需要的新增依赖。
  3. 自己重新启动前端服务（启动到 5173 端口）。
  4. 执行自测并验证功能是否正常。
  5. **自测完毕后，必须停掉刚才启动的 5173 端口进程。**
- **【当没有需要安装的新增依赖时】**：
  如果只是普通的组件/代码修改，意味着依赖热重载即可，**不要停掉** 5173 端口服务，也不要尝试重启！直接进行测试即可。

## 6. 组件选型与依赖管理规范 (Component & Dependency Rules)
> **强制红线：代码显示与高亮方案统一**
- 本项目**严禁**为了实现代码高亮而私自引入/安装任何新的第三方库（如 `prismjs`、`react-syntax-highlighter`、`codemirror` 等）。
- 绝不允许手动编写晦涩复杂的正则去自己造轮子实现关键字高亮。必须严格复用项目现存的“双引擎”架构：
  1. **纯代码的局部管理或 IDE 类交互（独立、重度的代码块视图）**：统一调用 `@monaco-editor/react` 及其内置支持。
  2. **在随记、长文笔记中穿插的代码块展示（富文本中的轻量混合）**：统一使用目前工程里的 `Tiptap` 以及其扩展生态的 `lowlight` 高亮机制。
