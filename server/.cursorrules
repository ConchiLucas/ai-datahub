# AI Backend Coding Rules & Skills

## 1. 基础说明
此文件旨在为 AI 代码助手提供针对 `ai-note/server`（后端）的工作规范与默认阅读上下文。

## 2. 核心架构与技术栈
- **语言**：Go
- **框架**：Gin Web Framework
- **ORM规范**：GORM（建议说明你的具体数据库如 PostgreSQL/MySQL 等）
- **路由层**：遵循 `router` -> `api` -> `service` -> `model` 的分层架构。

## 3. 专属规范 (Norms)
- **错误处理**：[请在此填写你们内部统一的错误抛出和日志记录标准]
- **实体设计**：[请在此填写关于数据库字段、ID生成方式的规范]
- **API标准**：[遵循 RESTful 或你们团队自己的响应体包装格式]

## 4. 特殊技能与避坑 (Skills & Gotchas)
- [填空] 例如：“在编写新模块时，请先检查 typical_errors.md”
- [填空] 例如：“请勿在此项目中使用特定的弃用第三方库”

## 5. AI 自测与执行策略 (Execution & Testing Skills)
> **非常重要：后端服务自测规范**
- **默认端口**：后端默认运行在 `8888` 端口。当我被要求“自测”时，会默认在此端口上发起请求或测试。
- **【当有代码修改，需要重启服务时】**：
  1. 先找出并杀死占用 `8888` 端口的进程（如 `lsof -i:8888` 后 `kill`）。
  2. 重新启动后端服务（如 `go run main.go`）。
  3. 进行自测并验证。
  4. **自测完毕后，必须杀掉刚才启动的 8888 端口服务进程。**
- **【当不需要重启后端时】**：
  如果操作不需要重启，**绝对不要**去执行杀死 8888 端口的动作，直接进行测试即可。

## 6. 表设计与业务规范 (Database & Business Logic Rules)
> **强制遵循的最优原则：**
- **建表必须带用身份关联**：以后凡是接到要求新增“导航栏类目”或者“新业务模块”的任务，创建的数据表**必须**包含 `user_id` 字段。
- **测试数据初始化**：当你编写完新模块的后端业务代码后，必须主动在库表中为 `user_id = 1` 的用户插入一些初始化测试数据。
- **全流程自测验证**：利用上述初始化数据，自己触发真实自测，确保该模块的 List 接口能查出数据，同时验证 Create (新增) 和 Update (修改) 流程顺畅。
- **代码硬编码红线**：业务逻辑代码中**绝对不允许**出现类似 `if admin == 1` 或是给 `user_id == 1` 开后门的特殊判断语句！所有的权限及资源过滤都应该走统一的、标准的鉴权体系。
- **文件存储与删除联动**：只要涉及文件/图片操作的任务，**强制默认对接 MinIO 服务**。在编写后端删除接口以清理含文件的业务数据时，除了删除本地表记录，**必须确保同步调用删除 MinIO 远端真实文件的逻辑**，严禁产生“孤儿死文件”。
