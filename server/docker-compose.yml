services:
  ai-file-navigation:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-file-navigation:1.0.0
    container_name: ai-file-navigation
    restart: unless-stopped
    ports:
      - "10001:10001"
    volumes:
      - ./log:/app/log
      - ./uploads:/app/uploads
